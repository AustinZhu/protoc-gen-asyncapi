# Regenerates the committed Go bindings for proto/temporal/v1/options.proto.
version: v2
inputs:
  - directory: proto
plugins:
  - local: protoc-gen-go
    out: gen
    opt:
      - module=github.com/AustinZhu/protoc-gen-temporal-asyncapi/gen
