# Code generated by protoc-gen-temporal-asyncapi. DO NOT EDIT.
# source: signup.proto
asyncapi: 3.1.0
id: urn:temporal:acme.signup.v1
info:
  title: acme.signup.v1
  version: 0.0.0
  description: |-
    Temporal Workflows, Activities, Signals, Queries and Updates, generated from Protobuf definitions by protoc-gen-temporal-asyncapi.

    Operations are described from the perspective of a Temporal client: the application that starts Workflows and sends them Signals, Queries and Updates (and the Workflows that execute Activities). Payloads use Temporal's `json/protobuf` encoding (canonical Protobuf JSON); request/reply primitives model their result as the operation's reply.
  tags:
    - name: Workflows
      description: Workflow types. Starting one returns immediately; its result is delivered on the reply channel when the execution completes.
      externalDocs:
        url: https://docs.temporal.io/workflows
defaultContentType: application/json
channels:
  workflow.Signup:
    address: workflow/Signup
    title: Signup
    description: Input of the Signup workflow.
    messages:
      SignupInput:
        $ref: '#/components/messages/SignupInput'
    tags:
      - name: Workflows
    bindings:
      $ref: '#/components/channelBindings/workflow.Signup'
operations:
  workflow.Signup:
    action: send
    channel:
      $ref: '#/channels/workflow.Signup'
    title: Signup
    summary: Start the Signup workflow.
    tags:
      - name: Workflows
    bindings:
      $ref: '#/components/operationBindings/workflow.Signup'
    messages:
      - $ref: '#/channels/workflow.Signup/messages/SignupInput'
components:
  channelBindings:
    workflow.Signup:
      x-temporal:
        kind: workflow
        name: Signup
        bindingVersion: 0.1.0
  operationBindings:
    workflow.Signup:
      x-temporal:
        kind: workflow
        name: Signup
        bindingVersion: 0.1.0
  schemas:
    acme.signup.v1.SignupInput:
      title: SignupInput
      type: object
      properties:
        email:
          type: string
          format: email
        username:
          type: string
          pattern: ^[a-z0-9_]+$
          minLength: 3
          maxLength: 32
        age:
          type: integer
          format: int32
          minimum: 13
          exclusiveMaximum: 130
        credits:
          type:
            - integer
            - string
          format: uint64
          pattern: ^[0-9]+$
          exclusiveMinimum: 0
        score:
          type: number
          minimum: 0.1
          maximum: 0.9
        tags:
          type: array
          items:
            type: string
            minLength: 1
          minItems: 1
          maxItems: 5
          uniqueItems: true
        quotas:
          type: object
          additionalProperties:
            type: integer
            format: int32
            minimum: 0
          maxProperties: 3
        plan:
          type: string
          enum:
            - free
            - pro
        phone:
          type: string
        slack:
          type: string
      required:
        - email
      oneOf:
        - required:
            - phone
        - required:
            - slack
  messages:
    SignupInput:
      name: SignupInput
      title: SignupInput
      contentType: application/json
      headers:
        description: Temporal Payload metadata.
        type: object
        properties:
          encoding:
            description: Temporal payload encoding.
            type: string
            const: json/protobuf
          messageType:
            description: Fully-qualified Protobuf message type.
            type: string
            const: acme.signup.v1.SignupInput
      payload:
        $ref: '#/components/schemas/acme.signup.v1.SignupInput'
