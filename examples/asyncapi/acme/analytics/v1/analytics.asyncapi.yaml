# Code generated by protoc-gen-asyncapi (devel). DO NOT EDIT.
# source: acme/analytics/v1/analytics.proto
asyncapi: 3.1.0
id: urn:acme:analytics
info:
  title: ACME Storefront Analytics
  version: 1.3.0
  description: |-
    Storefront analytics for the ACME shop, on Google Cloud Pub/Sub.

    The storefront publishes page views, ordered per visitor; a BigQuery
    subscription archives them, and the recommender consumes them from a pull
    subscription that dead-letters poison messages.
  contact:
    name: Data team
    email: data@acme.example
  tags:
    - name: Storefront
      description: Storefront tracking.
    - name: Recommender
      description: Product recommendations.
servers:
  production:
    host: pubsub.googleapis.com:443
    protocol: googlepubsub
    description: Google Cloud Pub/Sub.
    security:
      - $ref: '#/components/securitySchemes/google'
    bindings:
      x-googlepubsub:
        project: acme-shop
  emulator:
    host: localhost:8085
    protocol: googlepubsub
    description: '`gcloud beta emulators pubsub start`.'
    bindings:
      x-googlepubsub:
        project: acme-shop
        emulator: true
defaultContentType: application/json
channels:
  page-views:
    address: page-views
    messages:
      acme.analytics.v1.PageView:
        $ref: '#/components/messages/acme.analytics.v1.PageView'
    description: Every storefront page view.
    bindings:
      googlepubsub:
        messageRetentionDuration: 604800s
        schemaSettings:
          encoding: json
          name: projects/acme-shop/schemas/page-view
        bindingVersion: 0.2.0
      x-googlepubsub:
        topic: projects/acme-shop/topics/page-views
        subscriptions:
          - name: projects/acme-shop/subscriptions/page-views-recommender
            delivery: pull
            ackDeadlineSeconds: 30
            enableMessageOrdering: true
            deadLetterPolicy:
              deadLetterTopic: projects/acme-shop/topics/page-views.dead
              maxDeliveryAttempts: 5
            retryPolicy:
              minimumBackoff: 10s
              maximumBackoff: 600s
          - name: projects/acme-shop/subscriptions/page-views-warehouse
            delivery: bigquery
            bigqueryConfig:
              table: acme-shop.analytics.page_views
              useTopicSchema: true
              writeMetadata: true
  page-views.dead:
    address: page-views.dead
    bindings:
      x-googlepubsub:
        topic: projects/acme-shop/topics/page-views.dead
operations:
  Storefront.PageViewed:
    action: send
    channel:
      $ref: '#/channels/page-views'
    summary: Published for every page view, ordered per visitor.
    tags:
      - name: Storefront
    bindings:
      x-googlepubsub:
        orderingKey: '{visitor_id}'
        attributes:
          source: storefront
    messages:
      - $ref: '#/channels/page-views/messages/acme.analytics.v1.PageView'
    x-protobuf-method: acme.analytics.v1.Storefront.PageViewed
  Recommender.Learn:
    action: receive
    channel:
      $ref: '#/channels/page-views'
    summary: Updates the visitor's recommendations.
    tags:
      - name: Recommender
    bindings:
      x-googlepubsub:
        subscription: projects/acme-shop/subscriptions/page-views-recommender
        delivery: pull
        ordered: true
        flowControl:
          maxOutstandingMessages: 500
    messages:
      - $ref: '#/channels/page-views/messages/acme.analytics.v1.PageView'
    x-protobuf-method: acme.analytics.v1.Recommender.Learn
components:
  schemas:
    acme.analytics.v1.PageView:
      description: A page view.
      type: object
      properties:
        visitorId:
          description: Anonymous visitor ID.
          type: string
        path:
          type: string
        productId:
          description: Product shown on the page, if any.
          type: string
        viewedAt:
          type: string
          format: date-time
  messages:
    acme.analytics.v1.PageView:
      headers:
        type: object
        properties:
          source:
            type: string
            const: storefront
        required:
          - source
      payload:
        $ref: '#/components/schemas/acme.analytics.v1.PageView'
      name: PageView
      summary: A page view.
      bindings:
        googlepubsub:
          schema:
            name: projects/acme-shop/schemas/page-view
          bindingVersion: 0.2.0
      x-protobuf-message: acme.analytics.v1.PageView
  securitySchemes:
    google:
      type: oauth2
      description: Google service account credentials.
      flows:
        clientCredentials:
          tokenUrl: https://oauth2.googleapis.com/token
          availableScopes:
            https://www.googleapis.com/auth/pubsub: Publish and consume.
