# Code generated by protoc-gen-redis-asyncapi test. DO NOT EDIT.
# source: jobs/v1/jobs.proto
asyncapi: 3.0.0
info:
  title: jobs.v1
  version: 0.0.0
  description: Background jobs on Redis Streams and Lists.
  tags:
    - name: OrderEvents
      description: Order events on a stream consumed by a consumer group.
    - name: Mailer
      description: Work queues on lists.
    - name: Audit
      description: Audit trail on a stream read without a consumer group.
defaultContentType: application/json
channels:
  shop.orders.events:
    address: shop:orders:events
    messages:
      jobs.v1.OrderEvent:
        $ref: '#/components/messages/jobs.v1.OrderEvent'
    bindings:
      x-redis:
        type: stream
        entryField: payload
        consumerGroups:
          - name: fulfilment
            startId: "0"
        bindingVersion: 0.1.0
  shop.orders.events.dead:
    address: shop:orders:events:dead
    messages:
      jobs.v1.OrderEvent:
        $ref: '#/components/messages/jobs.v1.OrderEvent'
    description: 'Dead letters of `shop:orders:events`: entries delivered more than 5 times.'
    bindings:
      x-redis:
        type: stream
        entryField: payload
        bindingVersion: 0.1.0
  mail.outbox:
    address: mail:outbox
    messages:
      jobs.v1.Email:
        $ref: '#/components/messages/jobs.v1.Email'
    bindings:
      x-redis:
        type: list
        bindingVersion: 0.1.0
  mail.urgent:
    address: mail:urgent
    messages:
      jobs.v1.Email:
        $ref: '#/components/messages/jobs.v1.Email'
    bindings:
      x-redis:
        type: list
        bindingVersion: 0.1.0
  mail.render:
    address: mail:render
    messages:
      jobs.v1.RenderRequest:
        $ref: '#/components/messages/jobs.v1.RenderRequest'
    bindings:
      x-redis:
        type: list
        bindingVersion: 0.1.0
  mail.render.replies.request_id:
    address: mail:render:replies:{request_id}
    messages:
      jobs.v1.RenderResult:
        $ref: '#/components/messages/jobs.v1.RenderResult'
    parameters:
      request_id: {}
    bindings:
      x-redis:
        type: list
        bindingVersion: 0.1.0
  shop.audit:
    address: shop:audit
    messages:
      jobs.v1.AuditEntry:
        $ref: '#/components/messages/jobs.v1.AuditEntry'
    bindings:
      x-redis:
        type: stream
        entry: flattened
        bindingVersion: 0.1.0
operations:
  OrderEvents.Record:
    action: receive
    channel:
      $ref: '#/channels/shop.orders.events'
    summary: Records an order event.
    tags:
      - name: OrderEvents
    bindings:
      x-redis:
        command: XREADGROUP
        group: fulfilment
        bindingVersion: 0.1.0
    messages:
      - $ref: '#/channels/shop.orders.events/messages/jobs.v1.OrderEvent'
    x-protobuf-method: jobs.v1.OrderEvents.Record
  OrderEvents.Fulfil:
    action: send
    channel:
      $ref: '#/channels/shop.orders.events'
    summary: Fulfils orders.
    tags:
      - name: OrderEvents
    bindings:
      x-redis:
        command: XADD
        bindingVersion: 0.1.0
    messages:
      - $ref: '#/channels/shop.orders.events/messages/jobs.v1.OrderEvent'
    x-protobuf-method: jobs.v1.OrderEvents.Fulfil
  Mailer.Send:
    action: send
    channel:
      $ref: '#/channels/mail.outbox'
    summary: Sends an email (FIFO queue).
    tags:
      - name: Mailer
    bindings:
      x-redis:
        command: LPUSH
        bindingVersion: 0.1.0
    messages:
      - $ref: '#/channels/mail.outbox/messages/jobs.v1.Email'
    x-protobuf-method: jobs.v1.Mailer.Send
  Mailer.SendUrgent:
    action: send
    channel:
      $ref: '#/channels/mail.urgent'
    summary: Sends a priority email through a reliable queue.
    tags:
      - name: Mailer
    bindings:
      x-redis:
        command: RPUSH
        maxLen: 1000
        bindingVersion: 0.1.0
    messages:
      - $ref: '#/channels/mail.urgent/messages/jobs.v1.Email'
    x-protobuf-method: jobs.v1.Mailer.SendUrgent
  Mailer.Render:
    action: send
    channel:
      $ref: '#/channels/mail.render'
    summary: Renders a template; the reply is pushed to a per-request list.
    tags:
      - name: Mailer
    bindings:
      x-redis:
        command: LPUSH
        bindingVersion: 0.1.0
    messages:
      - $ref: '#/channels/mail.render/messages/jobs.v1.RenderRequest'
    reply:
      channel:
        $ref: '#/channels/mail.render.replies.request_id'
      messages:
        - $ref: '#/channels/mail.render.replies.request_id/messages/jobs.v1.RenderResult'
    x-protobuf-method: jobs.v1.Mailer.Render
  Audit.Tail:
    action: send
    channel:
      $ref: '#/channels/shop.audit'
    summary: Tails the audit stream.
    tags:
      - name: Audit
    bindings:
      x-redis:
        command: XADD
        trim:
          strategy: MINID
          threshold: 1700000000000-0
          exact: false
          limit: 100
        bindingVersion: 0.1.0
    messages:
      - $ref: '#/channels/shop.audit/messages/jobs.v1.AuditEntry'
    x-protobuf-method: jobs.v1.Audit.Tail
components:
  schemas:
    jobs.v1.OrderEvent:
      description: An order lifecycle event.
      type: object
      properties:
        orderId:
          type: string
        status:
          type: string
    jobs.v1.Email:
      description: An email.
      type: object
      properties:
        to:
          type: string
        subject:
          type: string
        body:
          type: string
    jobs.v1.RenderRequest:
      type: object
      properties:
        requestId:
          type: string
        template:
          type: string
    jobs.v1.RenderResult:
      type: object
      properties:
        html:
          type: string
    jobs.v1.AuditEntry:
      description: An audit log entry.
      type: object
      properties:
        actor:
          type: string
        action:
          type: string
        at:
          type: string
          format: int64
          pattern: ^-?[0-9]+$
  messages:
    jobs.v1.OrderEvent:
      payload:
        $ref: '#/components/schemas/jobs.v1.OrderEvent'
      name: OrderEvent
      summary: An order lifecycle event.
      x-protobuf-message: jobs.v1.OrderEvent
    jobs.v1.Email:
      payload:
        $ref: '#/components/schemas/jobs.v1.Email'
      name: Email
      summary: An email.
      x-protobuf-message: jobs.v1.Email
    jobs.v1.RenderRequest:
      payload:
        $ref: '#/components/schemas/jobs.v1.RenderRequest'
      name: RenderRequest
      x-protobuf-message: jobs.v1.RenderRequest
    jobs.v1.RenderResult:
      payload:
        $ref: '#/components/schemas/jobs.v1.RenderResult'
      name: RenderResult
      x-protobuf-message: jobs.v1.RenderResult
    jobs.v1.AuditEntry:
      payload:
        $ref: '#/components/schemas/jobs.v1.AuditEntry'
      name: AuditEntry
      summary: An audit log entry.
      x-protobuf-message: jobs.v1.AuditEntry
