# Code generated by protoc-gen-amqp-asyncapi test. DO NOT EDIT.
# source: acme/shipping/v1/shipping.proto
asyncapi: 3.1.0
id: urn:acme:shipping
info:
  title: ACME Shipping
  version: 2.1.0
  description: |-
    Shipping for the ACME shop, on RabbitMQ.

    Shipment events are published to the "shipping" topic exchange; the label
    printers consume them from a quorum queue that dead-letters failures.
    Checkout asks for shipping quotes over RPC.
  contact:
    name: Logistics team
    email: logistics@acme.example
  tags:
    - name: Shipments
      description: Shipment lifecycle events.
    - name: Quotes
      description: Shipping quotes for checkout.
servers:
  production:
    host: rabbitmq.acme.example:5671
    protocol: amqp
    description: RabbitMQ cluster.
    security:
      - $ref: '#/components/securitySchemes/user'
    bindings:
      x-amqp:
        vhost: shop
        tls: true
        heartbeat: 30
        bindingVersion: 0.3.0
  local:
    host: localhost:5672
    protocol: amqp
    description: '`rabbitmq-server` on your machine.'
defaultContentType: application/json
channels:
  queue.shipping.labels:
    address: shipping.labels
    messages:
      acme.shipping.v1.ShipmentCreated:
        $ref: '#/components/messages/acme.shipping.v1.ShipmentCreated'
    description: Shipments waiting for a label.
    bindings:
      amqp:
        is: queue
        queue:
          name: shipping.labels
          durable: true
          exclusive: false
          autoDelete: false
        bindingVersion: 0.3.0
        x-rabbitmq:
          queueType: quorum
          arguments:
            x-queue-type: quorum
            x-dead-letter-exchange: shipping.dlx
            x-delivery-limit: 3
          bindings:
            - exchange: shipping
              routingKey: shipment.created.*
  queue.shipping.failed:
    address: shipping.failed
    description: Shipments whose label could not be printed.
    bindings:
      amqp:
        is: queue
        queue:
          name: shipping.failed
          durable: true
          exclusive: false
          autoDelete: false
        bindingVersion: 0.3.0
        x-rabbitmq:
          queueType: classic
          arguments:
            x-message-ttl: 604800000
          bindings:
            - exchange: shipping.dlx
  shipping.shipment.created.carrier:
    address: shipment.created.{carrier}
    messages:
      acme.shipping.v1.ShipmentCreated:
        $ref: '#/components/messages/acme.shipping.v1.ShipmentCreated'
    parameters:
      carrier:
        description: Carrier code, e.g. "ups".
        location: $message.payload#/carrier
    bindings:
      amqp:
        is: routingKey
        exchange:
          name: shipping
          type: topic
          durable: true
          autoDelete: false
        bindingVersion: 0.3.0
  shipping.shipment.created.any:
    address: shipment.created.*
    messages:
      acme.shipping.v1.ShipmentCreated:
        $ref: '#/components/messages/acme.shipping.v1.ShipmentCreated'
    bindings:
      amqp:
        is: routingKey
        exchange:
          name: shipping
          type: topic
          durable: true
          autoDelete: false
        bindingVersion: 0.3.0
  default.shipping.quotes:
    address: shipping.quotes
    messages:
      acme.shipping.v1.QuoteRequest:
        $ref: '#/components/messages/acme.shipping.v1.QuoteRequest'
    bindings:
      amqp:
        is: routingKey
        exchange:
          name: ""
          type: default
        bindingVersion: 0.3.0
  queue.shipping.quotes:
    address: shipping.quotes
    messages:
      acme.shipping.v1.QuoteRequest:
        $ref: '#/components/messages/acme.shipping.v1.QuoteRequest'
    bindings:
      amqp:
        is: queue
        queue:
          name: shipping.quotes
        bindingVersion: 0.3.0
  queue.amq.rabbitmq.reply-to:
    address: amq.rabbitmq.reply-to
    messages:
      acme.shipping.v1.QuoteResponse:
        $ref: '#/components/messages/acme.shipping.v1.QuoteResponse'
    description: 'RabbitMQ direct reply-to: replies are delivered to the requester''s connection without a reply queue.'
    bindings:
      amqp:
        is: queue
        queue:
          name: amq.rabbitmq.reply-to
        bindingVersion: 0.3.0
operations:
  Shipments.Created:
    action: send
    channel:
      $ref: '#/channels/shipping.shipment.created.carrier'
    summary: Published when a shipment is created, per carrier.
    tags:
      - name: Shipments
    bindings:
      amqp:
        deliveryMode: 2
        mandatory: true
        bindingVersion: 0.3.0
        x-rabbitmq:
          publisherConfirms: true
    messages:
      - $ref: '#/channels/shipping.shipment.created.carrier/messages/acme.shipping.v1.ShipmentCreated'
    x-protobuf-method: acme.shipping.v1.Shipments.Created
  Shipments.PrintLabel:
    action: receive
    channel:
      $ref: '#/channels/queue.shipping.labels'
    summary: Prints the label of every new shipment.
    tags:
      - name: Shipments
    bindings:
      amqp:
        ack: true
        bindingVersion: 0.3.0
        x-rabbitmq:
          prefetch: 5
    messages:
      - $ref: '#/channels/queue.shipping.labels/messages/acme.shipping.v1.ShipmentCreated'
    x-protobuf-method: acme.shipping.v1.Shipments.PrintLabel
  Quotes.Quote:
    action: receive
    channel:
      $ref: '#/channels/queue.shipping.quotes'
    summary: Quotes the cheapest carrier for a parcel.
    tags:
      - name: Quotes
    bindings:
      amqp:
        expiration: 10000
        ack: true
        bindingVersion: 0.3.0
    messages:
      - $ref: '#/channels/queue.shipping.quotes/messages/acme.shipping.v1.QuoteRequest'
    reply:
      address:
        description: The reply_to property of the request names the queue replies are sent to, through the default exchange.
        location: $message.header#/reply_to
      channel:
        $ref: '#/channels/queue.amq.rabbitmq.reply-to'
      messages:
        - $ref: '#/channels/queue.amq.rabbitmq.reply-to/messages/acme.shipping.v1.QuoteResponse'
    x-protobuf-method: acme.shipping.v1.Quotes.Quote
components:
  schemas:
    acme.shipping.v1.ShipmentCreated:
      description: A new shipment.
      type: object
      properties:
        shipmentId:
          type: string
        carrier:
          description: Carrier code, e.g. "ups".
          type: string
        orderId:
          type: string
        createdAt:
          type: string
          format: date-time
    acme.shipping.v1.QuoteRequest:
      description: A parcel to quote.
      type: object
      properties:
        destinationCountry:
          type: string
        weightG:
          description: Weight in grams.
          type: integer
          format: uint32
          minimum: 0
    acme.shipping.v1.QuoteResponse:
      description: The cheapest quote.
      type: object
      properties:
        carrier:
          type: string
        priceCents:
          type: string
          format: int64
          pattern: ^-?[0-9]+$
  messages:
    acme.shipping.v1.ShipmentCreated:
      payload:
        $ref: '#/components/schemas/acme.shipping.v1.ShipmentCreated'
      name: ShipmentCreated
      summary: A new shipment.
      bindings:
        amqp:
          messageType: acme.shipping.v1.ShipmentCreated
          bindingVersion: 0.3.0
      x-protobuf-message: acme.shipping.v1.ShipmentCreated
    acme.shipping.v1.QuoteRequest:
      payload:
        $ref: '#/components/schemas/acme.shipping.v1.QuoteRequest'
      correlationId:
        description: The correlation_id property, copied from the request to its reply.
        location: $message.header#/correlation_id
      name: QuoteRequest
      summary: A parcel to quote.
      bindings:
        amqp:
          messageType: acme.shipping.v1.QuoteRequest
          bindingVersion: 0.3.0
      x-protobuf-message: acme.shipping.v1.QuoteRequest
    acme.shipping.v1.QuoteResponse:
      payload:
        $ref: '#/components/schemas/acme.shipping.v1.QuoteResponse'
      correlationId:
        description: The correlation_id property, copied from the request to its reply.
        location: $message.header#/correlation_id
      name: QuoteResponse
      summary: The cheapest quote.
      bindings:
        amqp:
          messageType: acme.shipping.v1.QuoteResponse
          bindingVersion: 0.3.0
      x-protobuf-message: acme.shipping.v1.QuoteResponse
  securitySchemes:
    user:
      type: userPassword
      description: RabbitMQ user of the shipping service.
      x-amqp-mechanism: PLAIN
