# Code generated by protoc-gen-temporal-asyncapi test. DO NOT EDIT.
# source: acme/signup/v1/signup.proto
asyncapi: 3.1.0
id: urn:temporal:acme.signup.v1
info:
  title: acme.signup.v1
  version: 0.0.0
  description: |-
    Temporal Workflows, Activities, Signals, Queries and Updates, generated from Protobuf definitions by protoc-gen-temporal-asyncapi.

    Operations are described from the perspective of a Temporal client: the application that starts Workflows and sends them Signals, Queries and Updates (and the Workflows that execute Activities). Payloads use Temporal's `json/protobuf` encoding (canonical Protobuf JSON); request/reply primitives model their result as the operation's reply.
  tags:
    - name: Workflows
      description: Workflow types. Starting one returns immediately; its result is delivered on the reply channel when the execution completes.
      externalDocs:
        url: https://docs.temporal.io/workflows
defaultContentType: application/json
channels:
  workflow.SignupWorkflow:
    address: workflow/SignupWorkflow
    title: SignupWorkflow
    description: Input of the SignupWorkflow workflow.
    messages:
      SignupInput:
        $ref: '#/components/messages/SignupInput'
    tags:
      - name: Workflows
    bindings:
      $ref: '#/components/channelBindings/workflow.SignupWorkflow'
operations:
  workflow.SignupWorkflow:
    action: send
    channel:
      $ref: '#/channels/workflow.SignupWorkflow'
    title: SignupWorkflow
    summary: Start the SignupWorkflow workflow.
    tags:
      - name: Workflows
    bindings:
      $ref: '#/components/operationBindings/workflow.SignupWorkflow'
    messages:
      - $ref: '#/channels/workflow.SignupWorkflow/messages/SignupInput'
components:
  channelBindings:
    workflow.SignupWorkflow:
      x-temporal:
        kind: workflow
        name: SignupWorkflow
        bindingVersion: 0.1.0
  operationBindings:
    workflow.SignupWorkflow:
      x-temporal:
        kind: workflow
        name: SignupWorkflow
        bindingVersion: 0.1.0
  schemas:
    acme.signup.v1.SignupInput:
      title: SignupInput
      type: object
      properties:
        email:
          type: string
        username:
          type: string
        age:
          type: integer
          format: int32
        credits:
          type:
            - integer
            - string
          format: uint64
          pattern: ^[0-9]+$
          minimum: 0
        score:
          type: number
        tags:
          type: array
          items:
            type: string
        quotas:
          type: object
          additionalProperties:
            type: integer
            format: int32
        plan:
          type: string
        phone:
          type: string
        slack:
          type: string
      oneOf:
        - required:
            - phone
        - required:
            - slack
        - not:
            anyOf:
              - required:
                  - phone
              - required:
                  - slack
  messages:
    SignupInput:
      name: SignupInput
      title: SignupInput
      contentType: application/json
      headers:
        description: Temporal Payload metadata.
        type: object
        properties:
          encoding:
            description: Temporal payload encoding.
            type: string
            const: json/protobuf
          messageType:
            description: Fully-qualified Protobuf message type.
            type: string
            const: acme.signup.v1.SignupInput
      payload:
        $ref: '#/components/schemas/acme.signup.v1.SignupInput'
