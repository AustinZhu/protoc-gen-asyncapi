# Code generated by protoc-gen-temporal-asyncapi test. DO NOT EDIT.
# source: acme/billing/v1/billing.proto
asyncapi: 3.1.0
id: urn:temporal:acme.billing.v1
info:
  title: acme.billing.v1
  version: 0.0.0
  description: |-
    Temporal Workflows, Activities, Signals, Queries and Updates, generated from Protobuf definitions by protoc-gen-temporal-asyncapi.

    Operations are described from the perspective of a Temporal client: the application that starts Workflows and sends them Signals, Queries and Updates (and the Workflows that execute Activities). Payloads use Temporal's `json/protobuf` encoding (canonical Protobuf JSON); request/reply primitives model their result as the operation's reply.
  tags:
    - name: Workflows
      description: Workflow types. Starting one returns immediately; its result is delivered on the reply channel when the execution completes.
      externalDocs:
        url: https://docs.temporal.io/workflows
defaultContentType: application/json
channels:
  workflow.CreateInvoice:
    address: workflow/CreateInvoice
    title: CreateInvoice
    description: Input of the CreateInvoice workflow.
    messages:
      CreateInvoiceInput:
        $ref: '#/components/messages/CreateInvoiceInput'
    tags:
      - name: Workflows
    bindings:
      $ref: '#/components/channelBindings/workflow.CreateInvoice'
  workflow.CreateInvoice.result:
    address: workflow/CreateInvoice/{workflowId}/result
    title: CreateInvoice result
    description: Result of the CreateInvoice workflow.
    messages:
      Invoice:
        $ref: '#/components/messages/Invoice'
    parameters:
      workflowId:
        description: Workflow ID of the target Workflow Execution.
    tags:
      - name: Workflows
    bindings:
      $ref: '#/components/channelBindings/workflow.CreateInvoice'
  workflow.BillShipment:
    address: workflow/BillShipment
    title: BillShipment
    description: Input of the BillShipment workflow.
    messages:
      ShipParcelInput:
        $ref: '#/components/messages/ShipParcelInput'
    tags:
      - name: Workflows
    bindings:
      $ref: '#/components/channelBindings/workflow.BillShipment'
  workflow.BillShipment.result:
    address: workflow/BillShipment/{workflowId}/result
    title: BillShipment result
    description: Result of the BillShipment workflow.
    messages:
      Invoice:
        $ref: '#/components/messages/Invoice'
    parameters:
      workflowId:
        description: Workflow ID of the target Workflow Execution.
    tags:
      - name: Workflows
    bindings:
      $ref: '#/components/channelBindings/workflow.BillShipment'
operations:
  workflow.CreateInvoice:
    action: send
    channel:
      $ref: '#/channels/workflow.CreateInvoice'
    title: CreateInvoice
    summary: Start the CreateInvoice workflow.
    description: Bills a customer.
    tags:
      - name: Workflows
    bindings:
      $ref: '#/components/operationBindings/workflow.CreateInvoice'
    messages:
      - $ref: '#/channels/workflow.CreateInvoice/messages/CreateInvoiceInput'
    reply:
      channel:
        $ref: '#/channels/workflow.CreateInvoice.result'
      messages:
        - $ref: '#/channels/workflow.CreateInvoice.result/messages/Invoice'
  workflow.BillShipment:
    action: send
    channel:
      $ref: '#/channels/workflow.BillShipment'
    title: BillShipment
    summary: Start the BillShipment workflow.
    description: Bills for a shipment; its input comes from another package.
    tags:
      - name: Workflows
    bindings:
      $ref: '#/components/operationBindings/workflow.BillShipment'
    messages:
      - $ref: '#/channels/workflow.BillShipment/messages/ShipParcelInput'
    reply:
      channel:
        $ref: '#/channels/workflow.BillShipment.result'
      messages:
        - $ref: '#/channels/workflow.BillShipment.result/messages/Invoice'
components:
  channelBindings:
    workflow.CreateInvoice:
      x-temporal:
        kind: workflow
        name: CreateInvoice
        bindingVersion: 0.1.0
    workflow.BillShipment:
      x-temporal:
        kind: workflow
        name: BillShipment
        bindingVersion: 0.1.0
  operationBindings:
    workflow.CreateInvoice:
      x-temporal:
        kind: workflow
        name: CreateInvoice
        bindingVersion: 0.1.0
    workflow.BillShipment:
      x-temporal:
        kind: workflow
        name: BillShipment
        bindingVersion: 0.1.0
  schemas:
    acme.billing.v1.CreateInvoiceInput:
      title: CreateInvoiceInput
      type: object
      properties:
        customerId:
          type: string
    acme.billing.v1.Invoice:
      title: Invoice
      type: object
      properties:
        id:
          type: string
    acme.shipping.v1.ShipParcelInput:
      title: ShipParcelInput
      type: object
      properties:
        address:
          type: string
  messages:
    CreateInvoiceInput:
      name: CreateInvoiceInput
      title: CreateInvoiceInput
      contentType: application/json
      headers:
        description: Temporal Payload metadata.
        type: object
        properties:
          encoding:
            description: Temporal payload encoding.
            type: string
            const: json/protobuf
          messageType:
            description: Fully-qualified Protobuf message type.
            type: string
            const: acme.billing.v1.CreateInvoiceInput
      payload:
        $ref: '#/components/schemas/acme.billing.v1.CreateInvoiceInput'
    Invoice:
      name: Invoice
      title: Invoice
      contentType: application/json
      headers:
        description: Temporal Payload metadata.
        type: object
        properties:
          encoding:
            description: Temporal payload encoding.
            type: string
            const: json/protobuf
          messageType:
            description: Fully-qualified Protobuf message type.
            type: string
            const: acme.billing.v1.Invoice
      payload:
        $ref: '#/components/schemas/acme.billing.v1.Invoice'
    ShipParcelInput:
      name: ShipParcelInput
      title: ShipParcelInput
      contentType: application/json
      headers:
        description: Temporal Payload metadata.
        type: object
        properties:
          encoding:
            description: Temporal payload encoding.
            type: string
            const: json/protobuf
          messageType:
            description: Fully-qualified Protobuf message type.
            type: string
            const: acme.shipping.v1.ShipParcelInput
      payload:
        $ref: '#/components/schemas/acme.shipping.v1.ShipParcelInput'
