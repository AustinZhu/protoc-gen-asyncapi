# Code generated by protoc-gen-temporal-asyncapi test. DO NOT EDIT.
# source: acme/signup/v1/signup.proto
asyncapi: 3.1.0
info:
  title: Signup
  version: 0.0.0
  tags:
    - name: Workflows
      description: Workflow types. Starting one returns immediately; its result is delivered on the reply channel when the execution completes.
      externalDocs:
        url: https://docs.temporal.io/workflows
    - name: Signup
defaultContentType: application/json
channels:
  workflow.SignupWorkflow:
    address: workflow/SignupWorkflow
    messages:
      acme.signup.v1.SignupInput:
        $ref: '#/components/messages/acme.signup.v1.SignupInput'
    title: SignupWorkflow
    description: Input of the SignupWorkflow workflow.
    bindings:
      x-temporal:
        kind: workflow
        name: SignupWorkflow
        bindingVersion: 0.1.0
operations:
  workflow.SignupWorkflow:
    action: receive
    channel:
      $ref: '#/channels/workflow.SignupWorkflow'
    summary: Start the SignupWorkflow workflow.
    tags:
      - name: Signup
      - name: Workflows
    bindings:
      x-temporal:
        kind: workflow
        name: SignupWorkflow
        bindingVersion: 0.1.0
    messages:
      - $ref: '#/channels/workflow.SignupWorkflow/messages/acme.signup.v1.SignupInput'
    x-protobuf-method: acme.signup.v1.Signup.SignupWorkflow
components:
  schemas:
    acme.signup.v1.SignupInput:
      type: object
      properties:
        email:
          type: string
          format: email
        username:
          type: string
          minLength: 3
          maxLength: 32
          pattern: ^[a-z0-9_]+$
        age:
          type: integer
          format: int32
          minimum: 13
          exclusiveMaximum: 130
        credits:
          type: string
          format: uint64
          pattern: ^[0-9]+$
        score:
          type: number
          format: float
          minimum: 0.1
          maximum: 0.9
        tags:
          type: array
          items:
            type: string
            minLength: 1
          minItems: 1
          maxItems: 5
          uniqueItems: true
        quotas:
          type: object
          additionalProperties:
            type: integer
            format: int32
            minimum: 0
          maxProperties: 3
        plan:
          type: string
          enum:
            - free
            - pro
        phone:
          type: string
        slack:
          type: string
      required:
        - email
      oneOf:
        - required:
            - phone
        - required:
            - slack
  messages:
    acme.signup.v1.SignupInput:
      headers:
        type: object
        properties:
          encoding:
            description: Temporal payload encoding.
            type: string
            const: json/protobuf
          messageType:
            description: Fully-qualified Protobuf message type.
            type: string
            const: acme.signup.v1.SignupInput
      payload:
        $ref: '#/components/schemas/acme.signup.v1.SignupInput'
      name: SignupInput
      x-protobuf-message: acme.signup.v1.SignupInput
