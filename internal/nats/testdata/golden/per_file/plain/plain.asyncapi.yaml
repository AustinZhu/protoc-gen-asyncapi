# Code generated by protoc-gen-nats-asyncapi test. DO NOT EDIT.
# source: plain/plain.proto
asyncapi: 3.1.0
info:
  title: Plain
  version: 0.0.0
  description: A proto2 service without annotations; included with include_all=true.
  tags:
    - name: Plain
      description: A proto2 service without annotations; included with include_all=true.
defaultContentType: application/json
channels:
  plain.plain.notify:
    address: plain.plain.notify
    messages:
      plain.Event:
        $ref: '#/components/messages/plain.Event'
  plain.plain.notify.reply:
    address: null
    messages:
      plain.Event:
        $ref: '#/components/messages/plain.Event'
    description: Replies to requests sent on `plain.plain.notify`. NATS delivers them to the reply subject (usually a unique `_INBOX` subject) chosen by the requester.
operations:
  Plain.Notify:
    action: receive
    channel:
      $ref: '#/channels/plain.plain.notify'
    tags:
      - name: Plain
    messages:
      - $ref: '#/channels/plain.plain.notify/messages/plain.Event'
    reply:
      channel:
        $ref: '#/channels/plain.plain.notify.reply'
      messages:
        - $ref: '#/channels/plain.plain.notify.reply/messages/plain.Event'
    x-protobuf-method: plain.Plain.Notify
components:
  schemas:
    plain.Event:
      description: A proto2 event.
      type: object
      properties:
        id:
          type: string
        count:
          type: integer
          format: int32
        level:
          $ref: '#/components/schemas/plain.Event.Level'
      required:
        - id
    plain.Event.Level:
      type: string
      enum:
        - LEVEL_LOW
        - LEVEL_HIGH
    plain.Unrelated:
      type: object
      properties:
        x:
          type: string
  messages:
    plain.Event:
      payload:
        $ref: '#/components/schemas/plain.Event'
      name: Event
      summary: A proto2 event.
      x-protobuf-message: plain.Event
