# Code generated by protoc-gen-nats-asyncapi test. DO NOT EDIT.
# source: shared/v1/nats.proto
asyncapi: 3.1.0
info:
  title: shared.v1
  version: 0.0.0
  description: Shared NATS settings imported by the other files.
servers:
  hub:
    host: '{region}.nats.example:4222'
    protocol: nats
    variables:
      region:
        enum:
          - eu
          - us
        default: eu
    security:
      - $ref: '#/components/securitySchemes/user'
      - $ref: '#/components/securitySchemes/token'
      - $ref: '#/components/securitySchemes/nkey'
      - $ref: '#/components/securitySchemes/mtls'
      - $ref: '#/components/securitySchemes/callout'
    x-nats:
      account: SHOP
      jetstreamDomain: hub
defaultContentType: application/json
components:
  securitySchemes:
    user:
      type: userPassword
      x-nats-auth: user_password
    token:
      type: apiKey
      in: user
      x-nats-auth: token
    nkey:
      type: asymmetricEncryption
      x-nats-auth: nkey
    mtls:
      type: X509
      x-nats-auth: tls
    callout:
      type: userPassword
      x-nats-auth: auth_callout
x-nats-jetstream:
  streams:
    INVENTORY:
      name: INVENTORY
      subjects:
        - inventory.*.reserve
      retention: workqueue
      max_bytes: 1073741824
      max_msgs_per_subject: 1
      max_msg_size: 65536
      discard: new
      storage: memory
      placement:
        cluster: eu
        tags:
          - ssd
      sources:
        - name: ARCHIVE
          filter_subject: archive.>
          external:
            api: $JS.edge.API
      compression: s2
      republish:
        src: '>'
        dest: mirror.>
        headers_only: true
      discard_new_per_subject: true
      allow_msg_ttl: true
      subject_delete_marker_ttl: 600000000000
      allow_atomic: true
      allow_msg_counter: true
      allow_msg_schedules: true
      metadata:
        owner: inventory
    AUDIT:
      name: AUDIT
      description: Subjects are derived from the bound channels.
  objectStores:
    images:
      bucket: images
      max_bytes: 10737418240
      storage: file
      replicas: 3
      compression: true
      stream: OBJ_images
