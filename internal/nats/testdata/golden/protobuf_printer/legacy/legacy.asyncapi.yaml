# Code generated by protoc-gen-nats-asyncapi test. DO NOT EDIT.
# source: legacy/legacy.proto
asyncapi: 3.1.0
info:
  title: Legacy
  version: 0.0.0
  description: A proto2 service without annotations; included with include_all=true.
  tags:
    - name: Legacy
      description: A proto2 service without annotations; included with include_all=true.
defaultContentType: application/protobuf
channels:
  legacy.legacy.notify:
    address: legacy.legacy.notify
    messages:
      legacy.Event:
        $ref: '#/components/messages/legacy.Event'
  legacy.legacy.notify.reply:
    address: null
    messages:
      legacy.Event:
        $ref: '#/components/messages/legacy.Event'
    description: Replies to requests sent on `legacy.legacy.notify`. NATS delivers them to the reply subject (usually a unique `_INBOX` subject) chosen by the requester.
operations:
  Legacy.Notify:
    action: receive
    channel:
      $ref: '#/channels/legacy.legacy.notify'
    tags:
      - name: Legacy
    messages:
      - $ref: '#/channels/legacy.legacy.notify/messages/legacy.Event'
    reply:
      channel:
        $ref: '#/channels/legacy.legacy.notify.reply'
      messages:
        - $ref: '#/channels/legacy.legacy.notify.reply/messages/legacy.Event'
    x-protobuf-method: legacy.Legacy.Notify
components:
  messages:
    legacy.Event:
      payload:
        schemaFormat: application/vnd.google.protobuf;version=2
        schema: |
          syntax = "proto2";

          package legacy;

          // A legacy event.
          message Event {
            required string id = 1;
            optional int32 count = 2 [default = 7];
            optional Event.Level level = 3 [default = LEVEL_HIGH];
            extensions 100 to max;

            enum Level {
              LEVEL_LOW = 0;
              LEVEL_HIGH = 1;
            }
          }
      name: Event
      summary: A legacy event.
      x-protobuf-message: legacy.Event
