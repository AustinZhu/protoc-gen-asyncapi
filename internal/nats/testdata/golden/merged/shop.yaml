# Code generated by protoc-gen-nats-asyncapi test. DO NOT EDIT.
asyncapi: 3.1.0
info:
  title: Billing
  version: 2.0.0
  description: Billing events.
  tags:
    - name: InventoryService
    - name: BillingService
      description: Invoices.
    - name: Plain
      description: A proto2 service without annotations; included with include_all=true.
servers:
  hub:
    host: '{region}.nats.example:4222'
    protocol: nats
    variables:
      region:
        enum:
          - eu
          - us
        default: eu
    security:
      - $ref: '#/components/securitySchemes/user'
      - $ref: '#/components/securitySchemes/token'
      - $ref: '#/components/securitySchemes/nkey'
      - $ref: '#/components/securitySchemes/mtls'
      - $ref: '#/components/securitySchemes/callout'
    x-nats:
      account: SHOP
      jetstreamDomain: hub
defaultContentType: application/protobuf
channels:
  inventory.warehouse.reserve:
    address: inventory.{warehouse}.reserve
    messages:
      inventory.v1.Reservation:
        $ref: '#/components/messages/inventory.v1.Reservation'
    servers:
      - $ref: '#/servers/hub'
    parameters:
      warehouse:
        location: $message.payload#/warehouse
    x-nats-jetstream:
      stream: INVENTORY
  inventory.count_stock:
    address: inventory.count_stock
    messages:
      inventory.v1.Reservation:
        $ref: '#/components/messages/inventory.v1.Reservation'
    servers:
      - $ref: '#/servers/hub'
  inventory.count_stock.reply:
    address: null
    messages:
      inventory.v1.StockLevel:
        $ref: '#/components/messages/inventory.v1.StockLevel'
      inventory.v1.Problem:
        $ref: '#/components/messages/inventory.v1.Problem'
    description: Replies to requests sent on `inventory.count_stock`. NATS delivers them to the reply subject (usually a unique `_INBOX` subject) chosen by the requester.
  inventory.audit:
    address: inventory.audit
    messages:
      inventory.v1.AuditEntry:
        $ref: '#/components/messages/inventory.v1.AuditEntry'
    servers:
      - $ref: '#/servers/hub'
    x-nats-jetstream:
      stream: AUDIT
  inventory.replay:
    address: inventory.replay
    messages:
      inventory.v1.AuditEntry:
        $ref: '#/components/messages/inventory.v1.AuditEntry'
    servers:
      - $ref: '#/servers/hub'
    x-nats-jetstream:
      stream: AUDIT
  lookup:
    address: inventory.lookup.{warehouse}
    messages:
      inventory.v1.Reservation:
        $ref: '#/components/messages/inventory.v1.Reservation'
    title: Lookups
    servers:
      - $ref: '#/servers/hub'
    parameters:
      warehouse:
        enum:
          - ams
          - fra
        default: ams
        description: Warehouse code.
        location: $message.payload#/warehouse
  inventory.lookup.replies:
    address: inventory.lookup.replies
    messages:
      inventory.v1.StockLevel:
        $ref: '#/components/messages/inventory.v1.StockLevel'
  billing.issue:
    address: billing.issue
    messages:
      billing.v1.Invoice:
        $ref: '#/components/messages/billing.v1.Invoice'
  billing.issue.reply:
    address: null
    messages:
      billing.v1.Invoice:
        $ref: '#/components/messages/billing.v1.Invoice'
    description: Replies to requests sent on `billing.issue`. NATS delivers them to the reply subject (usually a unique `_INBOX` subject) chosen by the requester.
  plain.plain.notify:
    address: plain.plain.notify
    messages:
      plain.Event:
        $ref: '#/components/messages/plain.Event'
  plain.plain.notify.reply:
    address: null
    messages:
      plain.Event:
        $ref: '#/components/messages/plain.Event'
    description: Replies to requests sent on `plain.plain.notify`. NATS delivers them to the reply subject (usually a unique `_INBOX` subject) chosen by the requester.
  inventory.alerts.low:
    address: inventory.alerts.low
    messages:
      inventory.v1.LowStock:
        $ref: '#/components/messages/inventory.v1.LowStock'
    servers:
      - $ref: '#/servers/hub'
  KV.stock.all:
    address: $KV.stock.>
    messages:
      inventory.v1.StockEntry:
        $ref: '#/components/messages/inventory.v1.StockEntry'
    x-nats-jetstream:
      stream: KV_stock
      keyValueBucket: stock
operations:
  InventoryService.Reserve:
    action: receive
    channel:
      $ref: '#/channels/inventory.warehouse.reserve'
    summary: Reserves stock.
    security:
      - $ref: '#/components/securitySchemes/user'
    tags:
      - name: InventoryService
    messages:
      - $ref: '#/channels/inventory.warehouse.reserve/messages/inventory.v1.Reservation'
    x-nats-jetstream:
      stream: INVENTORY
      consumerKind: push
      consumer:
        deliver_policy: by_start_time
        opt_start_time: "2025-01-01T00:00:00Z"
        filter_subject: inventory.*.reserve
        replay_policy: original
        deliver_subject: deliver.inventory
        flow_control: true
        idle_heartbeat: 5000000000
    x-protobuf-method: inventory.v1.InventoryService.Reserve
  InventoryService.CountStock:
    action: receive
    channel:
      $ref: '#/channels/inventory.count_stock'
    summary: Counts stock with the default subject.
    security:
      - $ref: '#/components/securitySchemes/token'
    tags:
      - name: InventoryService
    messages:
      - $ref: '#/channels/inventory.count_stock/messages/inventory.v1.Reservation'
    reply:
      channel:
        $ref: '#/channels/inventory.count_stock.reply'
      messages:
        - $ref: '#/channels/inventory.count_stock.reply/messages/inventory.v1.StockLevel'
        - $ref: '#/channels/inventory.count_stock.reply/messages/inventory.v1.Problem'
    x-protobuf-method: inventory.v1.InventoryService.CountStock
  InventoryService.Audit:
    action: send
    channel:
      $ref: '#/channels/inventory.audit'
    security:
      - $ref: '#/components/securitySchemes/user'
    tags:
      - name: InventoryService
    messages:
      - $ref: '#/channels/inventory.audit/messages/inventory.v1.AuditEntry'
    x-protobuf-method: inventory.v1.InventoryService.Audit
  InventoryService.Replay:
    action: receive
    channel:
      $ref: '#/channels/inventory.replay'
    security:
      - $ref: '#/components/securitySchemes/user'
    tags:
      - name: InventoryService
    messages:
      - $ref: '#/channels/inventory.replay/messages/inventory.v1.AuditEntry'
    x-nats-jetstream:
      stream: AUDIT
      consumerKind: pull
      consumer:
        durable_name: replayer
        filter_subjects:
          - audit.>
          - inventory.audit
        max_waiting: 128
        max_batch: 100
        max_expires: 30000000000
        max_bytes: 1048576
        inactive_threshold: 3600000000000
        priority_groups:
          - jobs
        priority_policy: pinned_client
        priority_timeout: 120000000000
        metadata:
          team: ops
    x-protobuf-method: inventory.v1.InventoryService.Replay
  InventoryService.Lookup:
    action: receive
    channel:
      $ref: '#/channels/lookup'
    summary: Static reply subject.
    security:
      - $ref: '#/components/securitySchemes/user'
    tags:
      - name: InventoryService
    messages:
      - $ref: '#/channels/lookup/messages/inventory.v1.Reservation'
    reply:
      channel:
        $ref: '#/channels/inventory.lookup.replies'
      messages:
        - $ref: '#/channels/inventory.lookup.replies/messages/inventory.v1.StockLevel'
    x-protobuf-method: inventory.v1.InventoryService.Lookup
  BillingService.Issue:
    action: receive
    channel:
      $ref: '#/channels/billing.issue'
    summary: Issues an invoice.
    tags:
      - name: BillingService
    messages:
      - $ref: '#/channels/billing.issue/messages/billing.v1.Invoice'
    reply:
      channel:
        $ref: '#/channels/billing.issue.reply'
      messages:
        - $ref: '#/channels/billing.issue.reply/messages/billing.v1.Invoice'
    x-protobuf-method: billing.v1.BillingService.Issue
  Plain.Notify:
    action: receive
    channel:
      $ref: '#/channels/plain.plain.notify'
    tags:
      - name: Plain
    messages:
      - $ref: '#/channels/plain.plain.notify/messages/plain.Event'
    reply:
      channel:
        $ref: '#/channels/plain.plain.notify.reply'
      messages:
        - $ref: '#/channels/plain.plain.notify.reply/messages/plain.Event'
    x-protobuf-method: plain.Plain.Notify
components:
  schemas:
    inventory.v1.Reservation:
      type: object
      properties:
        warehouse:
          type: string
        sku:
          type: string
          examples:
            - sku-1
        quantity:
          type: integer
          format: int32
          examples:
            - 2
    inventory.v1.StockLevel:
      type: object
      properties:
        available:
          type: integer
          format: int32
    inventory.v1.Problem:
      type: object
      properties:
        message:
          type: string
    inventory.v1.AuditEntry:
      type: object
      properties:
        text:
          type: string
    billing.v1.Invoice:
      type: object
      properties:
        id:
          type: string
        lines:
          type: array
          items:
            $ref: '#/components/schemas/billing.v1.Invoice.Line'
        currency:
          $ref: '#/components/schemas/billing.v1.Currency'
      required:
        - id
    billing.v1.Invoice.Line:
      type: object
      properties:
        text:
          type: string
        cents:
          type: string
          format: int64
          pattern: ^-?[0-9]+$
    billing.v1.Currency:
      type: string
      enum:
        - CURRENCY_UNSPECIFIED
        - CURRENCY_EUR
        - CURRENCY_EURO
    plain.Event:
      description: A proto2 event.
      type: object
      properties:
        id:
          type: string
        count:
          type: integer
          format: int32
        level:
          $ref: '#/components/schemas/plain.Event.Level'
      required:
        - id
    plain.Event.Level:
      type: string
      enum:
        - LEVEL_LOW
        - LEVEL_HIGH
    inventory.v1.LowStock:
      description: Emitted when stock runs low; not produced by an RPC.
      type: object
      properties:
        sku:
          type: string
    inventory.v1.StockEntry:
      description: Entries of the "stock" key-value bucket.
      type: object
      properties:
        available:
          type: integer
          format: int32
    plain.Unrelated:
      type: object
      properties:
        x:
          type: string
  messages:
    inventory.v1.Reservation:
      headers:
        type: object
        properties:
          Nats-Msg-Id:
            description: Unique message ID set by JetStream publishers. JetStream discards messages with an ID already seen within the duplicate window of the stream.
            type: string
          Nats-Expected-Last-Subject-Sequence:
            description: Expected sequence of the last message on the subject (optimistic concurrency control).
            type: string
          Nats-Expected-Stream:
            description: Name of the stream the message is expected to be stored in.
            type: string
          Nats-Batch-Id:
            description: Identifier of the atomic publish batch (NATS 2.12+).
            type: string
          Nats-Batch-Sequence:
            description: Sequence of the message within the batch, starting at 1.
            type: string
          Nats-Batch-Commit:
            description: Set to `1` on the last message to commit the batch.
            type: string
          Nats-Schedule:
            description: Schedule of the message, e.g. `@at 2030-01-01T00:00:00Z` or a cron expression (NATS 2.12+).
            type: string
          Nats-Schedule-Target:
            description: Subject the scheduled message is published to.
            type: string
      payload:
        $ref: '#/components/schemas/inventory.v1.Reservation'
      name: Reservation
      examples:
        - payload:
            sku: sku-1
            quantity: 2
      x-protobuf-message: inventory.v1.Reservation
    inventory.v1.StockLevel:
      payload:
        $ref: '#/components/schemas/inventory.v1.StockLevel'
      name: StockLevel
      x-protobuf-message: inventory.v1.StockLevel
    inventory.v1.Problem:
      payload:
        $ref: '#/components/schemas/inventory.v1.Problem'
      name: Problem
      x-protobuf-message: inventory.v1.Problem
    inventory.v1.AuditEntry:
      payload:
        $ref: '#/components/schemas/inventory.v1.AuditEntry'
      name: AuditEntry
      x-protobuf-message: inventory.v1.AuditEntry
    billing.v1.Invoice:
      payload:
        $ref: '#/components/schemas/billing.v1.Invoice'
      name: Invoice
      x-protobuf-message: billing.v1.Invoice
    plain.Event:
      payload:
        $ref: '#/components/schemas/plain.Event'
      name: Event
      summary: A proto2 event.
      x-protobuf-message: plain.Event
    inventory.v1.LowStock:
      payload:
        $ref: '#/components/schemas/inventory.v1.LowStock'
      correlationId:
        location: $message.header#/Nats-Msg-Id
      name: LowStock
      summary: Emitted when stock runs low; not produced by an RPC.
      x-protobuf-message: inventory.v1.LowStock
    inventory.v1.StockEntry:
      payload:
        $ref: '#/components/schemas/inventory.v1.StockEntry'
      name: StockEntry
      summary: Entries of the "stock" key-value bucket.
      x-protobuf-message: inventory.v1.StockEntry
  securitySchemes:
    user:
      type: userPassword
      x-nats-auth: user_password
    token:
      type: apiKey
      in: user
      x-nats-auth: token
    nkey:
      type: asymmetricEncryption
      x-nats-auth: nkey
    mtls:
      type: X509
      x-nats-auth: tls
    callout:
      type: userPassword
      x-nats-auth: auth_callout
x-nats-jetstream:
  streams:
    INVENTORY:
      name: INVENTORY
      subjects:
        - inventory.*.reserve
      retention: workqueue
      max_bytes: 1073741824
      max_msgs_per_subject: 1
      max_msg_size: 65536
      discard: new
      storage: memory
      placement:
        cluster: eu
        tags:
          - ssd
      sources:
        - name: ARCHIVE
          filter_subject: archive.>
          external:
            api: $JS.edge.API
      compression: s2
      republish:
        src: '>'
        dest: mirror.>
        headers_only: true
      discard_new_per_subject: true
      allow_msg_ttl: true
      subject_delete_marker_ttl: 600000000000
      allow_atomic: true
      allow_msg_counter: true
      allow_msg_schedules: true
      metadata:
        owner: inventory
    AUDIT:
      name: AUDIT
      description: Subjects are derived from the bound channels.
      subjects:
        - inventory.audit
        - inventory.replay
  keyValueBuckets:
    stock:
      bucket: stock
      max_value_size: 1024
      history: 1
      storage: memory
      stream: KV_stock
  objectStores:
    images:
      bucket: images
      max_bytes: 10737418240
      storage: file
      replicas: 3
      compression: true
      stream: OBJ_images
