# Code generated by protoc-gen-asyncapi test. DO NOT EDIT.
# source: mixed/v1/mixed.proto
asyncapi: 3.1.0
info:
  title: Orders
  version: 1.0.0
  description: |-
    Order processing that runs on Temporal, publishes events on NATS and
    queues receipts on Redis: three protocols in one AsyncAPI document.
  tags:
    - name: Workflows
      description: Workflow types. Starting one returns immediately; its result is delivered on the reply channel when the execution completes.
      externalDocs:
        url: https://docs.temporal.io/workflows
servers:
  nats:
    host: nats.example:4222
    protocol: nats
  temporal:
    host: temporal.example:7233
    protocol: temporal
    x-temporal-namespace: orders
  redis:
    host: redis.example:6379
    protocol: redis
defaultContentType: application/json
channels:
  workflow.ProcessOrder:
    address: workflow/ProcessOrder
    messages:
      mixed.v1.Order:
        $ref: '#/components/messages/mixed.v1.Order'
    title: ProcessOrder
    description: Input of the ProcessOrder workflow.
    servers:
      - $ref: '#/servers/temporal'
    bindings:
      x-temporal:
        kind: workflow
        name: ProcessOrder
        taskQueue: orders
        bindingVersion: 0.1.0
  workflow.ProcessOrder.result:
    address: workflow/ProcessOrder/{workflowId}/result
    messages:
      mixed.v1.OrderResult:
        $ref: '#/components/messages/mixed.v1.OrderResult'
    title: ProcessOrder result
    description: Result of the ProcessOrder workflow.
    servers:
      - $ref: '#/servers/temporal'
    parameters:
      workflowId:
        description: Workflow ID of the target Workflow Execution.
    bindings:
      x-temporal:
        kind: workflow
        name: ProcessOrder
        taskQueue: orders
        bindingVersion: 0.1.0
  orders.order_id.completed:
    address: orders.{order_id}.completed
    messages:
      mixed.v1.OrderResult:
        $ref: '#/components/messages/mixed.v1.OrderResult'
    servers:
      - $ref: '#/servers/nats'
    parameters:
      order_id:
        location: $message.payload#/orderId
  orders.receipts:
    address: orders:receipts
    messages:
      mixed.v1.OrderResult:
        $ref: '#/components/messages/mixed.v1.OrderResult'
    servers:
      - $ref: '#/servers/redis'
    bindings:
      x-redis:
        type: stream
        entryField: payload
        consumerGroups:
          - name: receipts
            startId: $
        bindingVersion: 0.1.0
operations:
  workflow.ProcessOrder:
    action: receive
    channel:
      $ref: '#/channels/workflow.ProcessOrder'
    summary: Processes an order.
    tags:
      - name: Workflows
    bindings:
      x-temporal:
        kind: workflow
        name: ProcessOrder
        taskQueue: orders
        bindingVersion: 0.1.0
    messages:
      - $ref: '#/channels/workflow.ProcessOrder/messages/mixed.v1.Order'
    reply:
      channel:
        $ref: '#/channels/workflow.ProcessOrder.result'
      messages:
        - $ref: '#/channels/workflow.ProcessOrder.result/messages/mixed.v1.OrderResult'
    x-protobuf-method: mixed.v1.OrderWorkflows.ProcessOrder
  Events.Completed:
    action: send
    channel:
      $ref: '#/channels/orders.order_id.completed'
    summary: Published when an order completes.
    messages:
      - $ref: '#/channels/orders.order_id.completed/messages/mixed.v1.OrderResult'
    x-protobuf-method: mixed.v1.Events.Completed
  Receipts.Send:
    action: receive
    channel:
      $ref: '#/channels/orders.receipts'
    summary: Sends the receipt of a completed order.
    bindings:
      x-redis:
        command: XREADGROUP
        group: receipts
        bindingVersion: 0.1.0
    messages:
      - $ref: '#/channels/orders.receipts/messages/mixed.v1.OrderResult'
    x-protobuf-method: mixed.v1.Receipts.Send
components:
  schemas:
    mixed.v1.Order:
      type: object
      properties:
        orderId:
          type: string
    mixed.v1.OrderResult:
      type: object
      properties:
        orderId:
          type: string
        ok:
          type: boolean
  messages:
    mixed.v1.Order:
      headers:
        type: object
        properties:
          encoding:
            description: Temporal payload encoding.
            type: string
            const: json/protobuf
          messageType:
            description: Fully-qualified Protobuf message type.
            type: string
            const: mixed.v1.Order
      payload:
        $ref: '#/components/schemas/mixed.v1.Order'
      name: Order
      x-protobuf-message: mixed.v1.Order
    mixed.v1.OrderResult:
      headers:
        type: object
        properties:
          encoding:
            description: Temporal payload encoding.
            type: string
            const: json/protobuf
          messageType:
            description: Fully-qualified Protobuf message type.
            type: string
            const: mixed.v1.OrderResult
      payload:
        $ref: '#/components/schemas/mixed.v1.OrderResult'
      name: OrderResult
      x-protobuf-message: mixed.v1.OrderResult
