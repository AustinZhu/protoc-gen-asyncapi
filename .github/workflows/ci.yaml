name: ci

on:
  push:
    branches: [main]
  pull_request:
  workflow_call:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - name: gofmt
        run: test -z "$(gofmt -l .)" || (gofmt -l . && exit 1)
      - run: go vet ./...
      - run: go test -race ./...

  generated:
    # The committed Go bindings and example output must match the sources.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - uses: bufbuild/buf-action@v1
        with:
          setup_only: true
      - run: go install google.golang.org/protobuf/cmd/protoc-gen-go@v1.36.12
      - run: go install .
      - run: buf lint
      - run: buf generate
      - run: buf generate example/proto --template example/buf.gen.yaml
      - name: Check for uncommitted changes
        run: git diff --exit-code

  asyncapi-spec:
    # Every golden document and the example must pass the official AsyncAPI parser.
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: tools/asyncapi-validate
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: tools/asyncapi-validate/package-lock.json
      - run: npm ci
      - run: node validate.mjs
      - run: node validate.mjs ../../example/gen/asyncapi.yaml
